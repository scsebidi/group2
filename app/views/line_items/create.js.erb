$('#cart').html("<%= escape_javascript(render(@cart)) %>");
$('#current_item').fadeTo(100, .1).delay(100).fadeTo(500, 1).css('background-color', '#FFFF99');
if ($('#cart tr').length == 1) { $('#cart').show('blind', 1000); };
$('#cd').slideDown(function() {
	$('#cart').slideDown();
});

